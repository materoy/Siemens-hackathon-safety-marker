import 'package:flutter/material.dart';

class MarkerAppBar extends StatelessWidget {
  const MarkerAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
